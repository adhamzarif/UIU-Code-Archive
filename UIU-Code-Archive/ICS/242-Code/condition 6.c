#include<stdio.h>
int main()
{
    int a;
    printf(" take input:");
    scanf("%d",&a);

    if(a > 0)
    {

        //power of 2 code
    }
    else {
        if(a==0){

        }
        else
        {

            //neg input not valid
        }
    }
    printf("negative input is not valid\n");

else if( a==0)
    printf("Zero is not a valid input");
else
    printf("yes\n");
    return 0;
}
